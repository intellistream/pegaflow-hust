# Contributing

Keep implementation, tests, experiments, paper assets, and reproducibility metadata owned by this repository. Keep mutable runtime dependencies as repo-local submodules or explicit patch deltas; do not depend on ambient sibling source trees for claim-bearing results.

Use a project-specific feature branch for runtime dependency changes, run the repository-owned test entrypoint before submission, and record exact parent/dependency commits plus dirty state in benchmark manifests.
